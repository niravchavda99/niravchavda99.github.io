/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    basePath: '/niravchavda99.github.io',
    distDir: 'dist',
    images: {
        unoptimized: true,
    },
}

export default nextConfig